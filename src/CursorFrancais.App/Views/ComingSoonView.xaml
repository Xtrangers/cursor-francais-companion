<UserControl x:Class="CursorFrancais.App.Views.ComingSoonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.Name="Écran bientôt">
    <StackPanel VerticalAlignment="Center" Margin="32">
        <TextBlock FontSize="22" FontWeight="SemiBold" Text="{Binding Title}" />
        <TextBlock Margin="0,10,0,0"
                   Style="{StaticResource MutedText}"
                   Text="{Binding Description}" />
        <Border Margin="0,20,0,0"
                Background="{StaticResource PanelBrush}"
                CornerRadius="8"
                Padding="16">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Bientôt" />
                <TextBlock Margin="0,8,0,0"
                           Style="{StaticResource MutedText}"
                           Text="Ce module arrive après le traducteur (phase 8). Rien à configurer pour l’instant." />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
